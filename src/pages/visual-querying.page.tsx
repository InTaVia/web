import { VisualQuerying } from '@/features/visual-querying/VisualQuerying';

export default function VisualQueryingPage(): JSX.Element {
  return (
    <main>
      <VisualQuerying />
    </main>
  );
}
